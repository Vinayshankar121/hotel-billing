export default function Header({ page, query, setQuery, dark, setDark, onMobileMenu, onNotify, connection }) {
    return <header className="topbar"><div style={{ display: 'flex', alignItems: 'center' }}><button className="mobile-menu" onClick={onMobileMenu}>☰</button><div><h1>{page === 'New Order' ? 'Create New Order' : page}</h1><div className="crumb">Good morning, Arjun <span className="muted">· Friday, 28 August 2026</span></div></div></div><div className="top-actions"><span className={`connection connection-${connection.kind}`} title={connection.message}>● {connection.label}</span><input className="search" placeholder="⌕  Search anything..." value={query} onChange={(event) => setQuery(event.target.value)} /><button className="icon-btn" onClick={() => onNotify('No new notifications')}>♧</button><button className="icon-btn" onClick={() => setDark(!dark)}>{dark ? '☀' : '☾'}</button><div className="date">10:48 AM<br /><b>Delhi, IN</b></div></div></header>
}
