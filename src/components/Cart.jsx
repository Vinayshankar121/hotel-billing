export default function Cart({ cart, tables = [], orderNumber, selectedTable, setSelectedTable, updateQuantity, subtotal, discount, cgst, total, onSave, onClear }) {
    return <aside className="panel cart-panel"><div className="order-code"><div><strong>Current order</strong><div className="muted">#{orderNumber} · Dine in</div></div><select className="table-select" value={selectedTable} onChange={(event) => setSelectedTable(event.target.value)}><option value="">Select table</option>{tables.map((table) => <option key={table[0]}>{table[0]}</option>)}</select></div><div className="cart-list">{cart.length ? cart.map((item) => <div className="cart-item" key={item.name}><div className="cart-index">{item.quantity}</div><div><strong>{item.name}</strong><span className="muted">₹{item.price} each</span></div><div className="qty"><button onClick={() => updateQuantity(item.name, -1)}>−</button><span>{item.quantity}</span><button onClick={() => updateQuantity(item.name, 1)}>＋</button></div><b style={{ fontSize: 11, width: 45, textAlign: 'right' }}>₹{item.price * item.quantity}</b></div>) : <p className="muted" style={{ textAlign: 'center', padding: 25 }}>Your cart is empty</p>}</div><div className="bill-lines"><div className="bill-line"><span>Subtotal</span><span>₹{subtotal}</span></div><div className="bill-line"><span>Discount</span><span>-₹{discount}</span></div><div className="bill-line"><span>CGST (5%)</span><span>₹{cgst}</span></div><div className="bill-line"><span>SGST (5%)</span><span>₹{cgst}</span></div><div className="bill-line total"><span>Grand total</span><span>₹{total}</span></div></div><div className="cart-actions"><button className="secondary" onClick={onClear}>Clear</button><button className="secondary" onClick={() => window.print()}>Print</button><button className="primary" onClick={onSave} disabled={!cart.length || !selectedTable}>Send to KOT</button></div></aside>
}
