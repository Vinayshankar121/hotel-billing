export default function Invoice({ receipt, onClose, onNewOrder }) {
    return <div className="modal-backdrop receipt-backdrop"><section className="receipt"><div className="receipt-head"><div><span className="receipt-kicker">PAYMENT RECEIPT</span><h2>TableMate Restaurant</h2><p>New Delhi · GSTIN 07AAACT1234A1Z5</p></div><button className="close" onClick={onClose}>×</button></div><div className="receipt-meta"><span>Invoice {receipt.invoice}</span><span>{receipt.date}</span><span>{receipt.table}</span></div><div className="receipt-items">{receipt.items.map((item) => <div className="bill-line" key={item.name}><span>{item.quantity} × {item.name}</span><span>₹{item.total}</span></div>)}</div><div className="bill-lines"><div className="bill-line"><span>Subtotal</span><span>₹{receipt.subtotal}</span></div><div className="bill-line"><span>Discount</span><span>-₹{receipt.discount}</span></div><div className="bill-line"><span>CGST + SGST</span><span>₹{receipt.tax}</span></div><div className="bill-line total"><span>Paid total</span><span>₹{receipt.total}</span></div></div><div className="receipt-paid"><span>PAID</span><b>{receipt.method.toUpperCase()}</b></div><p className="receipt-thanks">Thank you for dining with us.</p><div className="cart-actions"><button className="secondary" onClick={() => window.print()}>Print receipt</button><button className="primary" onClick={onNewOrder}>New order</button></div></section></div>
}
