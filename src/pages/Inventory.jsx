const defaultRows = [['Basmati Rice', 'Grains', '25 KG', '10 KG', 'In Stock'], ['Paneer', 'Dairy', '4 KG', '5 KG', 'Low Stock'], ['Chicken', 'Meat', '0 KG', '10 KG', 'Out of Stock'], ['Cooking Oil', 'Pantry', '18 L', '8 L', 'In Stock']]
export default function Inventory({ rows = defaultRows, onAdd }) { return <div className="page-grid"><div className="panel-head"><div><h2 className="section-title">Inventory</h2><p className="crumb">Know what is in stock before service begins</p></div><button className="primary" onClick={onAdd}>＋ Add stock</button></div><div className="metric-grid">{[['Total items', '128'], ['Low stock', '08'], ['Out of stock', '03'], ['Stock value', '₹2.84L']].map(([label, value]) => <div className="metric" key={label}><span className="muted">{label}</span><b>{value}</b></div>)}</div><section className="panel"><table className="data-table"><thead><tr><th>INGREDIENT</th><th>CATEGORY</th><th>CURRENT STOCK</th><th>MINIMUM</th><th>STATUS</th></tr></thead><tbody>{rows.map((row) => <tr key={row[0]}>{row.map((cell, index) => <td key={`${row[0]}-${index}`}>{index === 0 ? <b>{cell}</b> : index === 4 ? <span className={`badge ${cell === 'In Stock' ? 'green' : cell === 'Low Stock' ? 'orange' : 'red'}`}>{cell}</span> : cell}</td>)}</tr>)}</tbody></table></section></div> }
